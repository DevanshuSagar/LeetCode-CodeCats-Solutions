class Solution {
public:
    bool isStrictlyPalindromic(int n) {
        // There will never be a case when it will be true.
        // for every n greater then 4, number for base (n-2) = 12
        // which is not a palindrom "1100"
        return false;
    }
};
